# PDF OCR - Веб-сервис для распознавания финансовых отчетов

Веб-приложение для распознавания текста из PDF файлов с помощью OCR и экспорта результатов в Excel.

## Возможности

- 📄 Загрузка PDF файлов через веб-интерфейс
- 🔍 Распознавание текста с помощью EasyOCR (поддержка русского и английского языков)
- 📊 Экспорт результатов в Excel с координатами и уровнем уверенности
- 🎨 Современный и удобный интерфейс

## Установка

1. Установите зависимости:

```bash
pip3 install -r requirements.txt
```

**Важно**: EasyOCR требует установки дополнительных зависимостей. При первом запуске он автоматически загрузит необходимые модели.

2. Запустите приложение:

```bash
python3 app.py
```

3. Откройте в браузере:

```
http://localhost:5001
```

**Примечание**: Если порт 5001 занят, можно изменить его, установив переменную окружения `PORT`:
```bash
PORT=8080 python3 app.py
```

## Использование

1. Откройте веб-интерфейс в браузере
2. Загрузите PDF файл (перетащите или нажмите на область загрузки)
3. Нажмите кнопку "Обработать PDF"
4. Дождитесь завершения обработки
5. Файл Excel будет автоматически загружен

## Формат выходных данных

Excel файл содержит следующие колонки:
- `page` - номер страницы
- `x0`, `y0` - координаты левого верхнего угла распознанного текста
- `x1`, `y1` - координаты правого нижнего угла распознанного текста
- `text` - распознанный текст
- `confidence` - уровень уверенности распознавания (0-1)

## Технологии

- **Flask** - веб-фреймворк
- **EasyOCR** - библиотека для OCR
- **PyMuPDF (fitz)** - работа с PDF
- **OpenCV** - обработка изображений
- **Pandas** - обработка данных
- **openpyxl** - создание Excel файлов

## Примечания

- Максимальный размер файла: 50MB
- Первый запуск может занять время из-за загрузки моделей EasyOCR
- Обработка больших файлов может занять значительное время

